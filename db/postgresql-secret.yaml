apiVersion: v1
kind: Secret
metadata:
  name: postgresql-secret
type: Opaque
stringData:  # ← This would work!
  POSTGRES_PASSWORD: password123  # Plain text
  POSTGRES_USER: postgres         # Plain text
  POSTGRES_DB: myapp             # Plain text